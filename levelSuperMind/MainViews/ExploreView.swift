import SwiftUI

struct ExploreView: View {
    var body: some View {
        ZStack {
            Color(hex: "#090218").edgesIgnoringSafeArea(.all)
            Text("Explore View")
                .foregroundColor(.white)
                .font(.title)
        }
    }
}
